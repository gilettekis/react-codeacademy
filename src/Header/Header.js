import { Button } from '../Button/Button';
import "./header.css";

export const Header = () => {
    return (
        <div>
            <Button>Log in</Button>
            <Button>Register</Button>
        </div>
    )
}